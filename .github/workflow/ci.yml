name: "sample react ci pipline / workflow"

on:
  push:
     branches:
           - master

jobs:
   build-and-testS:
      runs-on: ubuntu-latest

      steps:
         - name: "setup node js"
           uses: actions/setup-node@v4
           with:
              node-version: '21'

